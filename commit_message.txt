feat: add child context and key insights toggles to GuidanceStructureSettings
    - Added enableChildContext and enableKeyInsights @AppStorage properties with default false
    - Added computed properties with proper objectWillChange notifications for SwiftUI reactivity
    - Added toggleChildContext() and toggleKeyInsights() convenience methods for UI interaction
    - Added hasEnabledPsychologistNotes computed property to check if either toggle is enabled
    - Maintains backward compatibility and follows existing patterns in the settings cla
