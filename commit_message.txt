fix: resolve OpenAI 400 errors by removing invalid JSON response format
- Remove response_format: { type: "json_object" } from analyze and context operations
- Add detailed error logging for OpenAI API responses with status and body
- Rely on existing JSON parsing with fallback logic instead of strict JSON mode
- Fixes 400 Bad Request errors causing analyze and regulation context extraction failures
