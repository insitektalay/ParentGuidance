refactor: extract all @State variables to SettingsViewState service class

- Create SettingsViewState.swift with 20 @Published properties from SettingsView
- Replace all individual @State declarations with single @StateObject reference
- Update all state variable references to use viewState prefix
- Maintain all existing functionality and localization keys
- Reduce SettingsView complexity by centralizing state management
- Build verified successful with no compilation errors
