refactor(SettingsView): complete Phase 3C confirmation dialog extractions
  - Extract SignOutConfirmationView component (42 lines)
  - Extract DeleteAccountConfirmationView component (184 lines)
  - Extract DeleteConfirmationTextField component (44 lines)
  - Fix ambiguous reference issue and clean up corrupted code
  - ConfirmationDialog already exists in Library/Components
  - Build successful with all components properly extracted, output a Bash script that:
      - Wraps the entire commit message in `echo '...' > commit_message.txt`, handling quotes and line breaks safely.
      - Runs:
         git add .
         git commit -F commit_message.txt
         git push

  Ensure:
  - All single quotes in the message are escaped (`'`)
  - Output is clean and copy-paste ready for macOS Terminal

  Only output the script — no explanation, no extra text.

